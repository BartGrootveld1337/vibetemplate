// Re-export for convenience
export { createClient } from './client'
export { createClient as createServerClient, createAdminClient } from './server'
export type { Database, Tables, InsertTables, UpdateTables, Profile } from './types'
